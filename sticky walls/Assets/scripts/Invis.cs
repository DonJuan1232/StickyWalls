using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Invis : MonoBehaviour
{

    /*public bool isInvis = false;

    float waitTime;


    public void Update()
    {
        waitTime = PlayerPrefs.GetFloat("WaitTime2");
    }

    void OnTriggerEnter(Collider other)
    {
        if(other.gameObject.tag == "invis")
        {
            Debug.Log("works");
            isInvis = true;
            Destroy(other.gameObject);
            StartCoroutine(invisOn());
        }

        if(other.gameObject.tag == "Enemy" && isInvis == true)
        {
            enableScript();
        }
    }

    



    IEnumerator invisOn()
    {
        yield return new WaitForSeconds(waitTime);

        isInvis = false;

   

        yield return 0;
    }

    public void enableScript()
    {
        StopCoroutine(invisOn());
        isInvis = false;
    }*/



}
